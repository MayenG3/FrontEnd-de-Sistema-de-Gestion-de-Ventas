{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/app",
    "types": ["bootstrap"] // Incluye el tipo de Bootstrap aquí
  },
  "files": [
    "src/main.ts"
  ],
  "include": [
    "src/**/*.ts",
    "src/bootstrap.d.ts" // Incluye el archivo de declaración si es necesario
  ]
}
